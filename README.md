# Radio

macOS menu bar radio player built with SwiftUI.

## Development

Open `Radio.xcodeproj` in Xcode and run the `Radio` scheme.

## Notes

- GitHub is used for source control and project history.
- The app is built locally from Xcode.
